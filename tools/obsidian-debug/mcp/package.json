{
  "name": "asp-obsidian-debug-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server (runs in the VM) that drives the ASP Debug Bridge plugin in Obsidian on the Mac, plus direct vault/plugin file ops over the OrbStack mount.",
  "scripts": {
    "start": "bun run server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  }
}
